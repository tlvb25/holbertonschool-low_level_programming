/**
 * _memset - function encodes a string using rot13
 * @s: input
 * @b: input
 * @n: input
 * Return: updated string
 */

void  *_memset(char *s, char b, unsigned int n)
{
	unsigned int i = 0;

	for (i = 0; i < n; i++)
	{
		s[i] = b;
	}

	return(s);
}
