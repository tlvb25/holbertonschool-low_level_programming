#include <stdio.h>
#include <string.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char str[50];

	strcpy(str, "\"Programming is like building a multilingual puzzle");
	puts(str);
	return (0);
}
