#include "holberton.h"
/**
 * add - function that adds 2 integers
 * @a: is one of the integers added together
 * @b: is one of the integers added together
 *
 * Description: Adds two integers
 * Return: The sum of the 2 integers is returned
 */
int add(int a, int b)
{
	return (a + b);
}
